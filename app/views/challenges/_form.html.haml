= simple_form_for(@challenge) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :checksum, disabled: true
    = f.input :question_count, as: :integer
    = f.input :trick_id, collection: Trick.all

  .form-actions
    = f.button :submit
